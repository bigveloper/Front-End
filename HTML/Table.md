# table 의 속성들
```HTML
<table>   : 테이블(표)을 만든다.
<tr>      : 테이블의 행(가로 한줄) 을 만든다.
<td>      : 테이블의 열 을 만든다.
<th>      : 테이블(표) 의 헤드 부분(자동으로 가운데 정렬, 굵게 적용)
<caption> : 테이블의 이름을 표시
<thead>   : 테이블의 헤더 영역을 지정
<hbody>   : 테이블의 바디 영역을 지정
```
